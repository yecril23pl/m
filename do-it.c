#include<stdlib.h>
#include<stdio.h>
int const main (int const, char const *const [])
{
enum MaxTry { N = 023 };
fprintf (stderr, "Counting numbers (n) up to %d that divide ((n 1 – +)!)…", N);
  return EXIT_FAILURE; }
